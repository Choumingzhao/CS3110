type time = int * int * string
(* let t : time = (10, 10, "am") *)
let t = (10, 10, "am")

let p = (5.0, 3.5)
type point = float * float

(* order of tuple element is relevant *)
(* usually 2 or 3 elements in reality usage *)
(* fst, snd *)